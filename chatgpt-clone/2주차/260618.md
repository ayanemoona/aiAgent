 챗메시지 휴먼안 에 챗메시지 ai를 못 넣음

 hosted tool 이미 내장 된 tool 
======
Image GenerationTool
image_placeholder = st.empty() 저장 공간 생성
response.image_generation_call.partial_image인 상태에서 그림을 조금씩 조금씩 그려줌
그 과정 상태를 또 update status로 과정을 보게 할 수 도 있음 

async def run_agent(message):
    yfinance_server = MCPServerStdio(
        params={
            "command":"uvx",
            "args" : ["mcp-yahoo-finance"]
        }
    )
    with yfinance_server:
        agent = Agent(
            mcp_servers=[
                yfinance_server,
            ],
        name="ChatGPT Clone",
        instructions = """
            You are a helpful assistant. 

            You have access to the following tools:
            - WebSearchTool: Use this when the user asks a question that isn't in your training data. Use this tool when the users asks about current or future events, when you thing you don't know the answer, try searching tor it in the web first.
            - FileSearchTool: Use this tool when the user asks a question about facts related to themselves. Or when they ask question about specific files
            - Code Interpreter Tool: Use this tool when you need to write and run code to answer the user's question.
            """,
        tools = [ WebSearchTool(), 
                FileSearchTool(vector_store_ids=[VECTOR_STORE_ID], max_num_results=3),
                ImageGenerationTool( tool_config = {
                        "type":"image_generation",
                        "quality":"low",
                        "output_format":"jpeg" ,
                        "moderation": "low",
                        "partial_images": 1
                    }, ),
                CodeInterpreterTool(
                    tool_config={
                        "type" : "code_interpreter",
                        "container":{"type":"auto",
                                    # "file_ids":[] 해당하는 파일을 접근 권한을 주는 거
                                    }
                    }
                ),
                HostedMCPTool(
                    tool_config={
                        "server_url" : "Https://mcp.context7.com/mcp",
                        "type":"mcp",
                        "server_label":"Context7",
                        "server_description" : "Use this to get the docs from software projects.",
                        "require_approval":"never"
                    }
                )
                    ]
        )
        agent = st.session_state["agent"]
        
    with st.chat_message("ai"):
        status_container = st.status("⏳", expanded=False)
        code_placeholder = st.empty()
        image_placeholder = st.empty()

        text_placeholder = st.empty()

이렇게 위치를 바꿈 mcp 서버를 추가 
연결할 서버를 만들고 
서버를 with 문으로 넣고 그 안에서 mcp 
근데 더이상 이런 캐싱 쓸 수 없음 mcp 서버는 서버에 라이브여야 해서
if agent not in st.session 안씀
서버를 시작해서 계속 돌아가게 해두고 agent 만들때 서버를 전달 해야 함
agent 캐싱을 중단 해야 함 
솔직히 여기 무슨 말인지 모르겠음 
 캐싱이 필요한 이유 : agent를 생성할 때 먼저 유저 정보를 찾아서 그걸 넘겨줘야 하는 경우
 agent를 호출하고 싶을 때마다 그때 그때 만들어주기 

 cache_tools_list= True tool 목록이 처음 조회될 때 캐시를 할 수 있음 
