그래프는 대부분 상태를 가진다 
상태(state)는 그래프를 통해 이동하는 데이터임 
타입딕셔너리 데이터 뭘 저장 할 건지 미리 지정
노드가 무엇을 리턴하든 그게 state에 들어가 사용
노드만이 state를 수정할 수 있음

=====
state 를 정리해서 divide and conquer 분할 정복할 수 있게 해줌
사용자가 접근할 수 없게
실행이 끝났을때 사용자가 그걸 보게 하고 싶지 않음
private state
Inputstate
Outputstate
그래프들이 이런 state를 사용한다고 선언하면
각 노드들은 원하는 state를 아무거나 요청할 수 있음 
각 노드들은 가진 모든 state를 업데이트 가능

node_five에서 어떻게 OutputState를 매개변수로 썼는데 return은 secret을 할 수 있나?
얘가 따로 class 메가ㅡㅡ 를 알지 못하고 걍 다음 노드에게 보낼 State를 업데이트 
출력과 입력 타입이 같을 필요 없다 

ode_six는 MegaPrivateState를 그래프에 등록하지 않았는데 왜 사용할 수 있나?
class MegaPrivateState(TypedDict):
    secret: bool
를 만들었으면 아무 함수에서나 사용할 수 있다.

graph.invoke 결과는 마지막 노드 return이 아니라 output_schema가 리턴

LangGraph는 실행 중에는 반환되는 키도 State에 추가해서 관리한다.

즉

처음에는

{
    a
    b
}

였는데

node5에서

return {
    "secret": True
}

를 하면

실행 중 State는

{
    a
    b
    secret
}

가 된다.

즉

실행 중 State는 PrivateState만 있는 것이 아니다.

그럼 PrivateState는 왜 쓰냐?

여기서 PrivateState는

"최소한 이런 키들은 존재한다."

를 알려주는 역할이다.

즉

PrivateState

a
b

↓

실행하다가

secret

추가 가능

↓

최종

a
b
secret
================
state는 기본적으로 덮어 써짐
추가 방법 state["massages"]+ 추가  
Annotated 로 덮어쓰기가 아닌 추가라는 거 명시

======
캐시로 같은 값을 계속 가지고 있을 수 있은 내가 설정한 시간 만큼 
========

한 노드에서 출발하는게 없으면 자동으로 END와 연결
=====
send api를 통해 노드를 동적으로 호출할 수 있음
동적으로 노드를 여러 번 실행하기 위해
고정된 흐름이 아니라,
실행 중에 "B 노드를 몇 번 실행할지" 결정할 수 있게 해줍니다
Send는 실행 중에 필요한 개수만큼 특정 노드를 동적으로 생성·실행
각 실행은 서로 다른 State를 받을 수 있습니다.